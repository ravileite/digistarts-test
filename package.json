{
  "name": "back-end",
  "version": "1.0.0",
  "description": "Digistarts back-end test.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.js",
    "start:dev": "nodemon src/index.js",
    "production": "pm2 start src/index.js --name backend"
  },
  "author": "Ravi Leite",
  "dependencies": {
    "express": "4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
